//给定两个整形变量的值，将两个值的内容进行交换
#include<stdio.h>
#include<stdlib.h>

int main(void)
{
	int x, y, z;
	printf("请输入要交换的两个数：");
	scanf("%d%d",&x,&y);
	z = x;
	x = y;
	y = z;
	printf("交换后两个数为%d\t%d\n",x,y);
	system("pause");
	return 0;
}
